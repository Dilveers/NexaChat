{
  "name": "nexachat",
  "private": true,
  "version": "0.1.0",
  "description": "Cloud-based real-time chat application using serverless backend architecture.",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "dependencies": {
    "emoji-picker-react": "^4.19.1",
    "firebase": "^12.15.0"
  },
  "overrides": {
    "next": {
      "postcss": "8.5.10"
    }
  }
}
